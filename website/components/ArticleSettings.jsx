import styles from 'website/styles/ArticleSettings.module.css'

export const ArticleSettings = ({
  /* config, */
  setScrollDistanceFactor,
}) => {
  return <div className={styles.container}></div>
}
